services:
  mongodb:
    image: mongo:7
    container_name: peoplepulse-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - peoplepulse_data:/data/db

volumes:
  peoplepulse_data:
